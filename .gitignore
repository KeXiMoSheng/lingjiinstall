# ===========================================================================
# 一键安装工具 —— Git 忽略清单
#
# 本仓库只入库源码与构建脚本。四个第三方安装包体积过大
# (LingeeSetup 约 258MB、dist 里的成品 exe 约 372MB,均超过 GitHub
#  单文件 100MB 硬上限),且随时可以重新获取,因此一律不入库。
# 打包 exe 前,请把这四个安装包放回仓库根目录(或根目录下的 installers
# 子目录,工具会自动到 exe 同目录及该子目录中查找安装包):
#   VC_redist.x64.exe
#   Git-2.54.0-64-bit.exe
#   TortoiseGit-2.18.0.1-64bit.msi
#   LingeeSetup_ia32_1.0.0_2608241831.exe
# ===========================================================================

# ---- 第三方安装包(仅打包时需要,不入库) ----
# 已默认忽略所有 exe/msi;若确需跟踪其中某个文件,用 git add -f <文件名>
*.exe
*.msi
installers/

# ---- PyInstaller 打包产物 ----
# build/ 与 dist/ 由 打包exe.bat 生成;spec 文件每次打包都会被删除重建,
# 属于生成物(内含安装包清单),无需入库
build/
dist/
*.spec
*.manifest

# ---- Python 运行缓存 ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# ---- 编辑器与操作系统文件 ----
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# ---- 临时文件与日志(本工具日志只显示在界面、不落盘,以下为防御性规则) ----
*.log
*.tmp
*.bak
~$*
